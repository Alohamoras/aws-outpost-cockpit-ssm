# Example environment configuration for aws-cockpit-outpost
# Copy this file to .env and update with your values

# AWS Configuration - Replace with your actual values
OUTPOST_ID=op-0123456789abcdef0
SUBNET_ID=subnet-0123456789abcdef0
SECURITY_GROUP_ID=sg-0123456789abcdef0
KEY_NAME=your-key-pair-name
INSTANCE_TYPE=c6id.metal
REGION=us-east-1

# SNS Topic ARN for email notifications (required)
# Replace with your actual SNS topic ARN
SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:your-topic-name

# SSM Document Configuration (optional override)
# Main orchestration document for modular installation
SSM_MAIN_DOCUMENT=cockpit-deploy-automation

# Deployment Options
# Continue deployment even if non-critical components fail
CONTINUE_ON_ERROR=true
# IAM role for automation execution (optional)
AUTOMATION_ASSUME_ROLE=

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual AWS configuration values
# 3. Run: ./launch-cockpit-instance.sh